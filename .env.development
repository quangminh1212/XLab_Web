# Development environment variables
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Note: NODE_TLS_REJECT_UNAUTHORIZED has been removed for security reasons
# If you need to bypass SSL verification in development, use a proper SSL certificate instead 