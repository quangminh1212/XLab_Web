# Development environment variables
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# SSL Configuration
# For development, use a valid SSL certificate
# Do not disable SSL verification as it makes connections insecure
# If you need to test with self-signed certificates, use proper SSL setup 