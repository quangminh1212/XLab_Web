# Security-related envs
# Admin emails (comma-separated)
ADMIN_EMAILS=admin@example.com

# Strong key for AES-256-GCM user data encryption
DATA_ENCRYPTION_KEY=change_me_to_a_strong_secret_key

# Strict env enforcement in production (1 to enable)
SECURE_STRICT_ENV=0

# Public API CORS allowlist (exact or wildcard origins)
# Example domains from your deployment (Vercel):
# - https://xlab.id.vn
# - https://xlabrnd.vercel.app
# - https://xlab-web-quangminh121200xlab.vercel.app
# - https://*.vercel.app (wildcard)
ALLOWED_ORIGINS=https://xlab.id.vn,https://xlabrnd.vercel.app,https://*.vercel.app

# Webhook paths that must skip CSRF (signature must be validated in route)
WEBHOOK_PATHS=

# Optional CSP whitelists (space-separated domains per directive, comma-separated here)
CDN_SCRIPT_HOSTS=
CDN_IMG_HOSTS=
CDN_STYLE_HOSTS=
CDN_FONT_HOSTS=
CONNECT_HOSTS=

